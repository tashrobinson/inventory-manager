import { WildcardSearchFilterPipePipe } from './wildcard-search-filter-pipe.pipe';

describe('WildcardSearchFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WildcardSearchFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
